title: ROS2 Parameter Practice
description: R터미널에서 `ros2 param set`을 손으로 치는 대신, Python 스크립트 하나가 3가지 색상을 3초 간격으로 자동 순환시킨다. 파라미터 CLI 명령어의 동작 방식을 코드로 자동화하는 연습이다.

tags:
  - ros2
  - beginner
  - pubsub
status: done
